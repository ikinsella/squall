{% for job in batch.jobs %}SPLICE {{ job.uid }} {{ launch_dir }}/{{ job.uid }}/{{ job.uid }}.dag
{% endfor %}
{% if batch.pre %}SCRIPT PRE pre{{ batch.name }} {{ launch_dir }}/{{ batch.share_dir }}/{{ batch.pre }}{% endif %}
{% if batch.post %}SCRIPT POST post{{ batch.name }} {{ launch_dir }}/{{ batch.share_dir }}/{{ batch.post }}{% endif %}
